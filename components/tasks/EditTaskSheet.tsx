'use client'
import type { Task } from '@/lib/types'

type Props = {
  task: Task | null
  onClose: () => void
  onSuccess: () => void
}

export default function EditTaskSheet(_props: Props) {
  return null
}
